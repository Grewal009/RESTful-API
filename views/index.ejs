<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All posts</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1>All posts</h1>
    <a class="new" href="http://localhost:8080/posts/new">Create New Post</a>
    <% for( post of posts) { %>
    <a class="link" href="http://localhost:8080/posts/<%= post.id %>">
      <div class="post">
        <h3 class="user">@<%= post.username %></h3>
        <h4 class="content"><%= post.content %></h4>
        <a class="edit" href="http://localhost:8080/posts/<%= post.id %>/edit"
          >edit</a
        >
        <a class="delete">delete</a>
      </div>
    </a>
    <% } %>
  </body>
</html>
